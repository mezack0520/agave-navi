# 生成物のパス。scripts/ci-push.sh が push 衝突からの復旧で「持ち越さない」対象。
# ここに挙げたものは data から build-all.sh が一意に作り直せるので、
# remote 側の版を捨てて再生成する。data ファイル(events.json など)は挙げない。
#
# 出どころは build-all.sh のチェーンが書き出す先。生成器を足したら
# ここにも足す。audit.py の ci_generated_paths_drift が
# 直近の chore(daily) コミットと突き合わせて漏れを検出する。
events/
feeds/
guides/
tag/
pref/
region/
# category/*.html は 2026-07-06 の bb64db4a8 で「手作りの静的ページ」から
# generate-landing-pages.py の日次生成に変わっている。にもかかわらず
# ここに無く、ci_generated_paths_drift が chore(daily) のたびに出していた。
# ci-push.sh はこれを「持ち越すデータ」と判定するので、衝突すると
# cancel-watch.json と同じ形で復旧が止まる(2026-09-15 に踏んだ型)。
# build-all で同一バイト再生成できることを確認済み(2026-09-16)。
category/
archive/
venue/
this-month/
this-weekend/
new/
index.html
calendar.html
map.html
sitemap.xml
rss.xml
events.csv
events.ics
this-month.ics
upcoming.ics
scripts/landing-meta.json
watch-sources.json
audit-results.json
audit-history.json
scripts/sitemap-lastmod.json
scripts/events-meta.json

# --- ここから下は build-all.sh では作り直せない。だが**持ち越さなくてよい**。---
# 外部を巡回した観測結果で、次の巡回が丸ごと書き換える。remote 側の版も
# 同じ性質なので、衝突したらこちらを捨てて構わない。捨てても失うのは
# 「その回の巡回結果」だけで、翌朝には同じものが入る。
#
# ここに無かったせいで、2026-09-15 の daily が cancel-watch.json の衝突で
# 落ちた。ci-push.sh がこれを「データファイル」と判定して復旧を諦め、
# その回の status 更新と再生成が丸ごと捨てられた。**毎回書き換わるものを
# 持ち越そうとすると、同時に走った回の数だけ衝突する。**
cancel-watch.json
coverage-gaps.json
# 2026-09-23 追加。主催者の Instagram の巡回結果。毎日まるごと書き換わる
organizer-posts.json

# --- 積み上げ式だが、作り直せる(2026-09-17) ---
# scripts/track-updates.py が build-all.sh の中で書く。TOPの更新欄とRSSの元。
# `git show HEAD:events.json` との差分を積むので、origin/main に作り直してから
# 回せば **remote 側の項目を読み込んだ上に自分の差分が乗る**。同じ
# (date, kind, slug) は二重に積まないので、2回走らせてもバイト一致する(実測)。
# ここに無かったせいで、2026-09-16 23:39 UTC の Daily Maintenance が
# 「データファイルが remote と衝突した」で落ちた。同時刻に sync-events が
# 新規3件を取り込んでおり、両方が同じ2ファイルへ項目を積んだため。
# cancel-watch.json と同じ型で、**毎回書き換わるものを持ち越そうとすると
# 同時に走った回の数だけ衝突する。**
site-updates.json
site-updates-scopes.json

# 手書きの13ページ。中身は人が書くが、フッター・パンくず・nav.js /
# outbound.js の読み込み・CSS/JSの版数は sync-footers.py が毎回入れ直す。
# **remote の版に乗せて build-all.sh を回すとCI側の変更は同じ形で戻る**
# ので判定は生成物側(2026-09-22 に about.html で実測して確認した)。
# ここに無いと、版数を上げた日に ci_generated_paths_drift が
# 13件まとめて鳴る。実際 2026-09-22 の outbound.js 導入で鳴った。
404.html
about.html
contact.html
dashboard.html
disclaimer.html
ikitai.html
listing.html
operator.html
privacy.html
terms.html
